<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <title>SQLite Web</title>
    <link href="/css/style.css" rel="stylesheet"/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/purecss@3.0.0/build/forms-min.css" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/purecss@3.0.0/build/buttons-min.css" crossorigin="anonymous">
  </head>
  <body>
    <header>
      <p class="crumbs">
        <a href="/">home</a>
      </p>
    </header>

      <% if (flash && flash.info)  { %>
        <% for (const message of flash.info) { %>
          <div class="flash flash-info">
            <%= message %>
          </div>
        <% } %>
      <% } %>

      <% if (flash && flash.success)  { %>
        <% for (const message of flash.success) { %>
          <div class="flash flash-success">
            <%= message %>
          </div>
        <% } %>
      <% } %>

      <% if (flash && flash.error)  { %>
        <% for (const message of flash.error) { %>
          <div class="flash flash-error">
            <%= message %>
          </div>
        <% } %>
      <% } %>
    <section class="content">
